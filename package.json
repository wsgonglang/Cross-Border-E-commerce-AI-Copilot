{
  "name": "cross-border-ecommerce-ai-copilot",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @cross-border/web",
    "dev:api": "npm run dev --workspace @cross-border/api",
    "dev:worker": "npm run dev --workspace @cross-border/worker",
    "build:workspace-deps": "npm run build --workspace @cross-border/shared && npm run prisma:generate --workspace @cross-border/api",
    "build": "npm run build --workspace @cross-border/shared && npm run build --workspace @cross-border/api && npm run build --workspace @cross-border/worker && npm run build --workspace @cross-border/web",
    "lint": "npm run build:workspace-deps && npm run lint:check",
    "lint:check": "eslint \"apps/**/*.{ts,tsx}\" \"packages/**/*.{ts,tsx}\"",
    "typecheck": "npm run build:workspace-deps && npm run typecheck:check",
    "typecheck:check": "npm run typecheck --workspaces --if-present",
    "test": "npm run build:workspace-deps && npm run test:check",
    "test:e2e": "npm run test:e2e --workspace @cross-border/web",
    "test:integration": "npm run test:integration --workspace @cross-border/api",
    "rag:eval": "npm run rag:eval --workspace @cross-border/api",
    "agent:eval:real": "npm run agent:eval:real --workspace @cross-border/api",
    "test:check": "npm run test --workspaces --if-present",
    "i18n:check": "node scripts/check-i18n.mjs",
    "format:check": "prettier --check \"apps/**/*.{ts,tsx,css,html,json}\" \"packages/**/*.{ts,tsx,json}\" \"docs/**/*.md\" \".github/**/*.{yml,yaml}\" \"*.{md,json,mjs,yml,yaml}\"",
    "verify": "npm run build:workspace-deps && npm run format:check && npm run lint:check && npm run typecheck:check && npm run i18n:check && npm run test:check && npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@types/node": "^24.0.0",
    "eslint": "^9.0.0",
    "eslint-plugin-react-hooks": "^7.0.0",
    "eslint-plugin-react-refresh": "^0.4.0",
    "globals": "^16.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.9.0",
    "typescript-eslint": "^8.0.0"
  }
}
