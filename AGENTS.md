# Cross-Border E-commerce AI Copilot 项目协作说明

## 1. 项目目标

本项目用于校招求职作品集和技术面试，目标是完成一个可运行、可测试、可讲清楚的“跨境电商 AI 运营助手”。

它不是把聊天框嵌入管理后台，而是让 AI 在明确权限、业务规则和人工确认机制下参与真实电商业务：

- 查询商品、SKU、库存、订单和经营数据；
- 生成或优化商品标题、描述和卖点；
- 翻译为英语、西班牙语和葡萄牙语，并适配目标市场；
- 检索平台规则、检查违规风险并提供可追溯引用；
- 生成商品修改草稿，经人工确认后再写入正式数据；
- 批量处理商品优化任务，并展示进度、失败明细和审计记录。
- 从工作台或任意核心业务页面发起带当前商家、店铺和资源上下文的 Agent 操作；
- 统一沉淀和检索 Agent 运行记录、商品优化草稿、批量结果及其后续处理状态；
- 通过 CSV/XLSX 导入结构化商品与 SKU，经过字段映射、校验预览和人工确认后再进入批量任务；
- 在一个商家下管理多个跨境店铺或销售渠道，并按店铺查看商品刊登、订单和经营数据；
- 提供可用于真实运营的会话整理、订单视图和待办工作台体验。

项目最终需要证明以下能力：

1. 能开发有权限、状态和复杂交互的 React 管理后台；
2. 能使用 NestJS、Prisma、MySQL 实现清晰的服务端业务；
3. 能正确实现流式输出、Structured Output、Tool Calling 和基础 RAG；
4. 能处理异步任务、幂等、审计、错误、测试和部署；
5. 能说明 AI 如何安全地参与业务，而不是只展示一个聊天页面。

## 2. 项目定位与取舍

### 2.1 核心定位

- 项目类型：AI 全栈业务应用；
- 目标用户：跨境电商商家运营人员；
- 目标规模：个人可完成、面试官可在较短时间内理解和运行；
- 架构原则：优先模块化单体，保持清晰边界，不为“看起来高级”增加复杂度；
- 完成标准：一条核心业务链路做深做完整，优先于大量只有页面或接口外壳的功能。

### 2.2 必须完成的核心演示链路

首要端到端链路为：

1. 用户登录并由服务端完成 RBAC 校验；
2. 用户查看商品与 SKU；
3. 用户在商品上下文中发起 AI 优化；
4. AI 流式反馈过程，并返回可校验的结构化草稿；
5. 页面展示原内容、草稿、风险和修改建议；
6. 用户人工确认；
7. 服务端通过正式 Product Service 写回商品；
8. 系统保存生成记录、前后版本、操作者和审计日志。

在该链路稳定之前，不优先扩展复杂 RAG、多个 Agent、外部电商平台接入或大规模基础设施。

核心链路稳定后的产品增强演示链路为：

1. 用户在全局上下文切换器中选择商家和店铺；
2. 工作台展示经营指标、待处理 AI 草稿、异常和后台 Agent 任务；
3. 用户从工作台或业务页面发起 Agent，运行过程和工具轨迹持久化；
4. Agent 生成的草稿或分析进入统一“AI 成果中心”，并可跳转到准确的商品、订单或批量任务；
5. 用户可通过 CSV/XLSX 上传商品数据，完成字段映射和校验预览后创建异步导入/优化任务；
6. 用户在成果中心完成审核，状态同步回工作台、商品页和任务详情；
7. 订单页面支持按支付、履约、退款、店铺和时间等维度建立运营视图。

### 2.3 明确不做或暂缓

除非后续任务明确要求，否则不要引入：

- 微服务拆分、服务网格、Kafka、Kubernetes；
- 通用多 Agent 编排平台或复杂自主规划框架；
- 自研向量数据库、自研工作流引擎；
- 亚马逊、Shopee 等真实平台的完整 OAuth 和生产级同步；
- 复杂财务、支付、物流、采购和仓储系统；
- 为所有表制作完整 CRUD；
- 只为展示设计模式而增加的抽象层；
- 没有真实业务入口的“技术演示型”功能。
- 将 Markdown、TXT 等非结构化文件直接转换并写入商品或 SKU；
- 未经字段映射、校验预览和人工确认的文件自动写入；
- 永久公开且无需鉴权的 AI 会话分享链接；
- 为导入功能自研通用 ETL 平台。

RAG、批量任务和 Agent 能力只实现足够支撑核心业务与面试讲解的版本。

## 3. 目标架构

采用一个仓库内的模块化单体，建议最终组织为：

```text
ai-chat/       原 AI 对话项目，只读参考，禁止修改
ecommerce-admin/
               原电商后台项目，只读参考，禁止修改
apps/
├── web/       React + TypeScript + Vite + Ant Design
├── api/       NestJS + Prisma + MySQL
└── worker/    BullMQ Worker（可与 API 共享业务模块）
packages/
└── shared/    仅放确有复用价值的类型、Schema 或配置
```

### 3.1 原项目只读约束

`ai-chat/` 和 `ecommerce-admin/` 是两个原始参考项目，必须保持原样：

- 禁止新增、修改、移动、重命名或删除这两个目录中的任何文件；
- 禁止在这两个目录中执行自动修复、格式化、依赖升级或生成构建产物；
- 禁止把融合后的新功能继续开发在这两个目录内；
- 需要复用的组件、类型、状态逻辑或交互，应复制到根目录下的新目标工程，再在新位置进行适配；
- 复制时只选择目标业务真正需要的部分，不整体照搬两个原项目；
- 若任务看起来需要修改原项目，必须改为在新工程中实现等价能力；确实无法绕开时，先停止并向项目所有者说明原因。

后续所有融合开发均在项目根目录下新建的 `apps/`、`packages/` 或其他明确属于目标项目的新目录中进行。两个原项目只用于阅读、对照和验证可复用思路，不计入目标工程的可写范围。

如果一次性建立完整目标结构成本过高，可以在根目录分阶段创建；不要在一次任务中大规模复制并同时改写全部业务。

主要边界：

- Web 负责交互体验、权限提示和状态展示，但不作为最终权限边界；
- Controller 负责协议、参数校验和响应，不承载大段业务逻辑；
- Service 负责业务规则、事务和流程编排；
- Repository/Prisma 负责数据访问；
- AI Provider 负责模型适配、流式输出、结构化结果、超时、取消和用量信息；
- Agent Tool 必须调用现有业务 Service，不允许绕过 Service 直连数据库；
- Worker 负责批量任务，MySQL 保存最终状态，Redis 只保存队列、缓存和短期进度。

保持单一 AI Provider 接口，并至少提供一个可测试的 Mock Provider。只有在真实需要第二个供应商时才实现复杂的多供应商路由。

## 4. 核心业务约束

### 4.1 角色

- `admin`：管理用户、角色、商家、规则知识库和全部审计记录；
- `operator`：管理商品与 SKU、查看订单、使用 AI、创建批量任务；
- `viewer`：只读查看商品、订单和报表。

所有权限必须由服务端校验。前端路由和按钮权限只用于改善体验。

### 4.2 AI 写操作

- AI 默认只能读取业务数据或生成草稿；
- 正式商品内容只能在人工确认后写入；
- 写操作必须校验权限和输入，支持幂等，并记录审计日志；
- AI 不得直接操作 Prisma 或绕过 Product Service；
- 保存生成前后的版本，确保结果可回溯；
- 结构化商品优化结果至少包含：

```ts
interface ProductOptimizationDraft {
  title: string
  description: string
  sellingPoints: string[]
  complianceRisks: string[]
  suggestions: string[]
  language: string
  confidence: number
}
```

### 4.3 RAG

- 规则回答必须返回来源；
- 检索不足时明确说明信息不足，不得编造平台规则；
- 第一版可使用小型、可替换的检索实现；
- 文档权限至少按商家或全局规则范围过滤；
- 在有可验证收益前，不实现复杂 rerank 管线。

### 4.4 批量任务

- 批量 AI 优化不得在单个 HTTP 请求中同步执行；
- 使用 BullMQ，支持进度、失败重试、失败明细和取消未执行任务；
- 必须避免重复消费导致重复写入；
- Redis 中的状态不是最终业务事实，最终结果要写入 MySQL。

## 5. 现有两个原项目的完成程度

基线日期：2026-07-26。

启动融合前，根目录只有两个并列的原型项目，没有统一的根级工程配置。两个原项目的能力基线固定记录如下；融合工程的最新进度以第 6 节为准。

这里的“原项目完成程度”衡量两个项目相对于它们原本的原型目标完成了多少，不等于融合目标项目的完成程度。

### 5.1 `ecommerce-admin` 原项目完成程度

已存在：

- React、TypeScript、Vite、Ant Design、Redux Toolkit、Axios、ECharts；
- 登录、仪表盘、商品、订单、用户、设置和个人信息页面；
- `admin`、`operator`、`viewer` 的前端路由/菜单控制；
- 商品、订单、用户的列表和部分表单交互；
- 中英文文案和主题设置；
- Vite Mock 接口与演示数据。

局限：

- 当前是前端管理后台原型，业务接口主要由 `vite-plugin-mock` 提供；
- 没有 NestJS、Prisma、MySQL、服务端 JWT/RBAC 和真实审计；
- 没有商家、SKU 独立业务、AI 页面、任务中心和规则知识库；
- 前端角色控制不能视为真实安全边界；
- 尚未发现项目测试、E2E、Docker 或 CI。

| 原后台能力         | 完成程度 | 说明                                                                                                                      |
| ------------------ | -------- | ------------------------------------------------------------------------------------------------------------------------- |
| 管理后台布局与导航 | 基本完成 | 已有侧栏、嵌套路由、个人信息、设置和 403 页面                                                                             |
| 登录与角色体验     | 原型完成 | 可演示登录、前端路由和菜单角色控制，数据来自 Mock                                                                         |
| 数据看板           | 原型完成 | 已有概览、订单、销售和图表展示，数据来自 Mock                                                                             |
| 商品管理           | 原型完成 | 已有列表、分页筛选和新增/编辑交互，缺少真实后端、商家隔离和 SKU 完整业务                                                  |
| 订单管理           | 原型完成 | 已有列表、筛选、详情和部分状态交互，缺少真实状态机与事务                                                                  |
| 用户管理           | 原型完成 | 已有列表和角色交互，缺少真实 JWT、Refresh Token 与服务端 RBAC                                                             |
| 国际化与主题       | 基本完成 | 已有中英文文案和主题配置                                                                                                  |
| 自动化测试         | 未开始   | 未发现单元测试、集成测试或 E2E                                                                                            |
| 工程质量验收       | 未完成   | 2026-07-26 执行 lint 为 39 errors、5 warnings；TypeScript 构建阶段通过，但 Vite 在当前沙箱中因 `spawn EPERM` 未能完成验证 |

原后台项目结论：**前端 Mock 演示原型已成形，但还不是可作为真实业务后端使用的全栈系统。**

### 5.2 `ai-chat` 原项目完成程度

已存在：

- React、TypeScript、Vite、Ant Design 和 Redux Toolkit；
- 流式消息读取与增量渲染；
- `AbortController` 停止生成；
- 会话管理、本地持久化、消息树与分支切换；
- 消息修改、版本、收藏、分享和导出相关交互；
- 一个 Express + TypeScript 的轻量模型代理，包含聊天和标题生成接口。

局限：

- 会话和消息主要保存在浏览器本地，不是服务端持久化；
- Express 服务不是目标 NestJS 业务后端；
- 模型配置仍与具体兼容接口耦合，尚无完整 Provider 抽象；
- 没有 Structured Output、业务 Tool Calling、RAG、用量审计和人工确认写回；
- 没有商品、订单、权限等业务上下文；
- 尚未发现项目测试、Docker 或 CI。

| 原聊天能力         | 完成程度 | 说明                                                                                        |
| ------------------ | -------- | ------------------------------------------------------------------------------------------- |
| 基础对话 UI        | 基本完成 | 已有输入、消息列表、Markdown 渲染与会话侧栏                                                 |
| 流式输出与停止生成 | 基本完成 | 已实现流读取、缓冲渲染和 `AbortController` 取消                                             |
| 会话与历史记录     | 原型完成 | 支持创建、切换、重命名等，但主要依赖浏览器本地持久化                                        |
| 消息树与分支       | 基本完成 | 已有父子消息结构、分支与版本切换逻辑                                                        |
| 收藏、分享与导出   | 原型完成 | 已有对应前端交互，未形成服务端共享能力                                                      |
| AI 服务端          | 部分完成 | Express 服务可代理聊天与标题生成，但不是目标 NestJS 后端，也缺少业务级 Provider、鉴权和审计 |
| AI 业务能力        | 未开始   | 没有商品优化 Structured Output、Tool Calling、RAG 和人工确认写回                            |
| 自动化测试         | 未开始   | 未发现单元测试、集成测试或 E2E                                                              |
| 工程质量验收       | 未完成   | 2026-07-26 执行 build 因未使用的导入未通过，lint 为 29 errors、2 warnings                   |

原聊天项目结论：**聊天交互能力较完整，可作为融合时的功能资产，但服务端、持久化和电商业务能力仍是原型级。**

### 5.3 原项目能力总览

| 维度             | `ecommerce-admin`                      | `ai-chat`                            |
| ---------------- | -------------------------------------- | ------------------------------------ |
| 可复用重点       | 后台布局、业务页面、图表、前端权限体验 | 流式交互、会话状态、消息树、停止生成 |
| 当前成熟度       | 前端 Mock 原型                         | 前端功能原型 + 轻量模型代理          |
| 真实服务端业务   | 未完成                                 | 仅有非业务化 Express 代理            |
| 数据持久化       | Mock/浏览器状态                        | 浏览器本地状态                       |
| 测试与交付       | 未完成                                 | 未完成                               |
| 对融合项目的意义 | 提供后台壳和页面资产                   | 提供 AI 对话交互资产                 |

### 5.4 安全提醒

仓库中存在 `ai-chat/openaikey.txt`。不得读取、复制、提交或继续依赖该文件。若其中曾保存真实密钥，应由项目所有者立即在供应商后台撤销/轮换。后续密钥只通过未提交的环境变量提供，并维护不含真实值的 `.env.example`。

## 6. 融合目标项目的完成程度

状态只使用：`未开始`、`进行中`、`已完成`、`受阻`。不得仅因存在页面或 Mock 就把业务阶段标记为已完成。

| 阶段                  | 范围                                                     | 当前状态 | 当前说明                                                                                                               |
| --------------------- | -------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------- |
| 0. 资产盘点与范围收敛 | 梳理两个原型、确定核心链路与非目标                       | 已完成   | 已形成本文档，后续需随实际进度更新                                                                                     |
| 1. 融合工程骨架       | 根级工作区、Web/API/Worker、共享配置、环境校验、健康检查 | 已完成   | 已建立 npm workspaces、React Web、NestJS API、Worker、共享环境 Schema；`GET /api/health` 已通过真实启动验证            |
| 2. 认证与权限         | User、Role、JWT、Refresh Token、服务端 RBAC、登录接入    | 已完成   | 已实现 MySQL/Prisma 用户与角色模型、JWT、Refresh Token 轮换与撤销、登录日志、全局服务端鉴权和 RBAC；Web 已接入真实登录、会话恢复及管理员用户页，真实数据库联调通过 |
| 3. 商家、商品与 SKU   | 数据模型、CRUD、分页筛选、库存、审计                     | 已完成   | 已实现 Merchant/MerchantUser/Product/SKU/AuditLog 模型、商家隔离 CRUD、商品分页筛选、SKU 与原子库存调整；Web 已接入真实商家和商品管理，写操作事务审计、负库存与跨商家失败路径均通过验证 |
| 4. 订单与看板         | 订单查询、状态约束、经营数据接口                         | 已完成   | 已实现 Order/OrderItem Prisma 模型与迁移；订单 CRUD 与分页筛选；状态机约束（8 状态 9 允许转换）、角色权限验证；经营看板概览（今日订单/销售额/商品数/库存预警）、14 天趋势图、销售分析与订单分析；前后端真实连通，5 条演示种子订单；自动化测试覆盖商家隔离、状态拒绝、角色权限、聚合指标和本地时区日期归档 |
| 5. AI 会话底座迁移    | 服务端会话、消息树、流式输出、停止生成、历史记录         | 已完成   | 已实现 AiSession/AiMessage Prisma 模型与迁移；NestJS AI 模块（AiProvider 抽象、Mock + OpenAI 兼容）、严格 DTO 校验、流式 chat、会话 CRUD；前端支持会话历史、消息流和停止生成；消息树父子关系与服务端时间戳持久化，停止时保存部分回复；自动化测试和真实联调覆盖用户隔离、流式持久化、停止生成、未知字段 400 与 viewer 403 |
| 6. 单商品 AI 优化闭环 | 结构化草稿、翻译、合规风险、人工确认、版本与用量审计     | 已完成   | 已实现 ProductOptimization/ProductVersion 模型和三语结构化草稿；Web 展示原文对比、卖点、风险、建议、置信度与用量，人工确认后经 Product Service 事务写回；支持版本冲突、重复确认幂等、拒绝草稿、前后版本和审计；自动化测试及真实 MySQL 联调覆盖成功、viewer 403、过期草稿 409 和拒绝路径 |
| 7. Agent Tool Calling | 商品/SKU/订单/经营数据查询、规则检索、创建草稿           | 已完成   | 已实现单 Agent 与 6 个白名单工具：商品、库存、订单、经营概览、规则知识库检索、创建优化草稿；模型参数经 Zod 服务端校验，工具只调用业务 Service，执行结果逐次审计；订单结果裁剪敏感字段，草稿工具需明确用户意图且每次最多一次，不提供正式写回工具；Web 展示模式切换、快捷指令和工具轨迹；viewer 只能获得只读工具，operator/admin 才能创建草稿；自动化测试、真实 MySQL 与浏览器联调覆盖成功、非法参数、规则来源、审计及草稿不改正式商品 |
| 8. 批量 AI 任务       | Redis、BullMQ、Worker、进度、重试、幂等、取消            | 已完成   | 已实现 BatchOptimizationTask/Item MySQL 模型、Redis/BullMQ 队列和独立 Worker；每商品独立 Job，默认重试 3 次，以任务项 ID 和草稿唯一关联防止重复消费；API 支持商家隔离、最多 20 商品、幂等创建、分页详情和取消未执行项，Web 任务中心展示持久化进度、尝试次数和失败明细；批量流程只生成草稿不自动写回。自动化测试与真实 MySQL/Redis/Worker/浏览器联调覆盖完成、重复提交、改参冲突 409、viewer 403、取消及正式商品版本不变 |
| 9. 规则 RAG           | 文档导入、切分、检索、引用、权限过滤和基础评估           | 已完成   | 已实现 RuleDocument/RuleChunk MySQL 模型；管理员可导入 Markdown/纯文本并按全局或当前商家作用域保存、查看和归档，写操作审计；服务端按标题和段落确定性切分，使用可替换的中英文词法评分器返回引用编号、文档/分块 ID、平台、作用域、来源、摘录和分数，低于阈值明确返回信息不足；Agent 规则工具已改用真实规则 Service。固定评估集 Recall@3=1.0，自动化测试与真实 MySQL/API/Agent/浏览器联调覆盖全局+商家过滤、viewer 只检索不管理、重复导入 409、归档退出检索、未知问题拒答和审计 |
| 10. 工程化与交付      | 测试、日志、Request ID、Docker、CI、README、演示数据     | 已完成   | 已建立统一 `npm run verify`（格式、lint、类型、106 项测试、生产构建）；API 接入安全 Request ID、结构化完成日志及 Web 错误关联；Docker `app` profile 已真实验证 MySQL、Redis、迁移、幂等演示种子、API、Worker、Nginx Web 全栈启动，登录、会话恢复、健康检查和 Swagger 连通；GitHub Actions 使用真实 MySQL/Redis 执行迁移与 verify，并分别构建 API/Worker/Web 镜像；README、架构说明和 8 分钟面试演示文档已补齐。依赖审计还包含 React Router RSC Action 公告，以及 ExcelJS 生成侧依赖链和开发工具链的 `brace-expansion` 公告；当前只有破坏性降级/升级修复方案，作为非阻塞上游风险持续跟踪 |
| 11. AI 成果中心       | AgentRun 持久化、统一成果索引、状态筛选、业务深链接      | 已完成   | 已实现 AgentRun/AgentToolCall MySQL 模型、迁移和成功/失败/工具轨迹持久化；成果中心按商家聚合 Agent 运行与 ProductOptimization，支持类型、状态和分页筛选，并可精确跳转商品草稿或批量任务；Agent 面板提供成果中心及新草稿入口，工作台展示待确认草稿数和近期成果；真实 Compose/API/浏览器联调验证刷新后记录存在、草稿深链接、跨商家 403，自动化测试覆盖失败安全落库、商家范围和状态过滤 |
| 12. 店铺与全局上下文  | SalesChannel/Store、商品刊登、商家/店铺切换、演示数据    | 已完成   | 已实现 Store/ProductListing 模型、复合商家外键、CRUD 与审计；订单和 AgentRun 可关联店铺，商品、订单、看板和 Agent 工具按全局所选商家/店铺过滤；Web 新增持久化业务上下文栏和店铺刊登管理页。种子提供 Amazon 美国店、Shopee 巴西店及每店三条刊登，订单按 3/2 分配；自动化测试与真实 MySQL/API/浏览器联调覆盖 operator 管理、同商品多店铺、币种约束、上下文联动、跨店订单拒绝和跨商家 403 |
| 13. Agent 运营工作台  | 完整经营看板、待办、异常、快捷 Agent、后台任务与近期成果 | 已完成   | 已实现店铺级 7/14/30 天运营聚合接口和等长上一周期对比，展示销售额、订单、客单价、退款、趋势、状态分布、热销商品与低库存；待办聚合待处理订单、待确认草稿、失败任务和低库存，并提供业务依据与操作入口；工作台展示运行中 Agent/批量任务、近期成果及继承当前商家/店铺/时间范围的快捷 Agent。viewer 仅获只读工具，operator/admin 可创建草稿；自动化测试与真实 MySQL/API/浏览器验收覆盖指标复算、店铺隔离、非法店铺 404、30 天/店铺切换、工具轨迹和窄屏布局 |
| 14. AI 会话生产力     | 重命名、置顶、搜索、分组、归档、收藏、导出和内部分享     | 已完成   | 已实现服务端持久化的重命名、人工标题保护、置顶、标题/消息搜索、分组、归档/恢复和归档后二次确认永久删除；消息可收藏并按商品编码或订单号关联商家内业务对象；Markdown/JSON 导出和内部分享快照统一脱敏邮箱、电话与收货地址。分享仅授权当前商家有效用户，支持有效期、撤销和访问审计，创建者与收件人可访问只读页面；自动化测试及真实 MySQL/API/浏览器验收覆盖归档隔离、删除前置条件、业务关联、脱敏、跨用户 404、撤销/过期 410 和人工标题不被自动命名覆盖 |
| 15. 结构化导入中心    | CSV/XLSX 上传、解析、字段映射、预览、校验、任务和失败文件 | 已完成   | 已实现 CSV/XLSX 文件分析、工作表/表头选择、字段映射、商品与 SKU 行级预览，以及有效、无效、警告和覆盖风险提示；限制 5 MB、10 个工作表、1000 行和 80 列，不执行公式、宏或外部链接。ImportJob/Item 将最终状态持久化到 MySQL，BullMQ Worker 支持逐行重试、幂等消费和取消未执行项，所有写入通过 Product/SKU/Optimization Service 并保留审计；支持“仅导入草稿”和“导入后创建 AI 优化”，失败明细可下载 CSV，成果中心可精确跳转任务。97 项自动化测试及真实 MySQL/Redis/API/Worker/浏览器验收覆盖预览零写入、有效/无效行并存、JSON 映射键序无关幂等、取消、AI 草稿和结果深链接；商品导入不支持 Markdown |
| 16. 订单运营工作台    | 多维状态、保存视图、组合筛选、列设置、批量操作和时间线   | 已完成   | 已在原生命周期状态外增加支付、履约、退款金额、物流与乐观版本，并将现有 Store 作为销售渠道边界，避免重复建模；订单列表支持关键词、生命周期、支付、履约、时间、金额和店铺组合筛选，用户保存视图可恢复筛选、排序、列与分页配置。批量操作逐单校验角色、状态机、商家隔离和幂等，部分失败返回逐单结果；状态、时间线和审计在同一事务内写入。宽 Drawer 展示商品、客户、地址、金额、物流和事件时间线，并内嵌只读取脱敏订单投影的业务 Agent。工作台待处理订单深链使用与服务端统计一致的 `PENDING/CONFIRMED/REFUNDING` 视图；102 项测试及真实 MySQL/API/浏览器验收覆盖保存视图往返、组合筛选、角色拒绝、状态拒绝、批量部分失败、重复请求、冲突请求、事件追溯和 Agent 不暴露邮箱/地址 |
| 17. Web 测试与性能基础 | 前端测试、路由懒加载、统一加载态和拆包验证             | 已完成   | 已接入 Vitest、Testing Library、jest-dom 和 jsdom，新增 4 项认证/角色路由测试并自动纳入根级 test/CI；全部页面和 AppLayout 使用路由级懒加载，加载态不依赖 Ant Design 且支持减少动态效果；生产构建从单一约 1.37 MB 主包拆为独立页面/布局 chunk，最大 JS chunk 375.02 kB，不再出现 500 kB 大包提示；格式、lint、类型、测试和构建通过 |
| 18. 订单页模块化      | 查询、保存视图、批量操作、表格、详情和时间线拆分         | 未开始   | 先建立测试安全网，再在不改变业务行为的前提下拆分千行页面 |
| 19. AI 对话页模块化   | 会话导航、消息区、输入区、会话操作和分享拆分             | 未开始   | 保持流式取消、历史、收藏、归档和分享行为不变 |
| 20. 服务端状态治理    | TanStack Query、缓存、失效、竞态、轮询和错误策略          | 未开始   | 只迁移商家/店铺、商品、订单、任务和成果等高价值服务端状态 |
| 21. 后台国际化        | 中文/英文界面、金额日期本地化、界面语言设置              | 未开始   | 先覆盖登录、导航、工作台和商品优化核心链路 |
| 22. AI 质量度量       | 接受率、工具成功率、时延、Token、失败率和轻量质量视图    | 未开始   | 指标必须可追溯并按商家隔离，不把小型固定评估集夸大为生产效果 |
| 23. 作品集呈现        | README 首屏、截图、短演示、架构图和三步启动              | 未开始   | 一分钟可理解、三分钟可观看核心链路，不暴露密钥或个人数据 |

目标项目当前结论：**阶段 0 至阶段 17 已完成并可运行。项目已经具备完整业务和 AI 闭环，以及首批 Web 自动化测试和路由级性能基础；下一阶段按 P0 顺序拆分订单页，在测试安全网下收敛查询、保存视图、批量操作、详情和时间线职责。后续不扩大业务领域。**

注意：第 5 节中的“原型完成”不能折算成这一节中的“目标阶段已完成”。例如，后台已有商品 Mock 页面，不代表目标项目的商品与 SKU 阶段已经完成；聊天项目已有本地会话，也不代表目标项目已经完成服务端会话管理。

不要使用虚假的总体百分比掩盖不同模块的完成差异。对外介绍时应分别说明“原项目资产成熟度”和“融合目标完成度”。

## 7. 开发优先级

按以下顺序推进：

1. 建立可运行的融合工程骨架；
2. 完成真实登录、服务端 RBAC 和基础数据库；
3. 打通商家、商品、SKU 与订单的真实读取；
4. 迁移 AI 会话底座；
5. 完成单商品 AI 优化的端到端闭环；
6. 增加只读 Tool Calling，再增加“创建草稿”写工具；
7. 增加批量任务；
8. 增加最小可靠 RAG；
9. 完成交付级测试、Docker、CI 和文档。
10. 建立 AI 成果中心和 AgentRun 持久化，先解决所有 AI 结果的去向与深链接；
11. 增加店铺/销售渠道模型和全局商家/店铺上下文；
12. 升级工作台并接入上下文 Agent、待办和近期成果；
13. 补齐会话整理、收藏、归档、导出和受控内部分享；
14. 增加 CSV/XLSX 结构化导入、映射预览和异步任务；
15. 升级订单多维状态、保存视图、批量操作和详情时间线。
16. 建立 Web 测试安全网并完成路由级懒加载；
17. 依次拆分订单页和 AI 对话页，不改变业务行为；
18. 治理高价值服务端状态，再补核心链路中英文界面；
19. 增加轻量 AI 质量度量，最后完善作品集首屏与短演示。

每次任务只推进一个可验证的小阶段。若任务没有明确指定范围，优先选择能缩短核心演示链路的最小改动。

## 8. 代码与工程规则

- 开始修改前先阅读相关现有代码、配置和本文件；
- 将 `ai-chat/` 和 `ecommerce-admin/` 视为只读目录，任何任务均不得改动；
- 所有新增实现必须位于项目根目录下新建的目标工程目录中；
- 优先复用两个原型中经过验证的交互与组件，不机械复制整个项目；
- Web 页面、布局和有独立视觉职责的组件采用 TSX/CSS 同目录共置；根级样式只保留 reset、设计基础和真正跨模块的共享规则，不新增静态内联样式；
- 保持 TypeScript 严格类型，避免新增 `any`；
- 所有外部输入在服务端校验；
- 金额使用数据库 Decimal 或最小货币单位，禁止浮点金额；
- 多商家数据查询必须带 `merchantId` 隔离条件；
- 重要状态变化使用明确状态机或允许转换表；
- 环境变量必须校验，并同步维护 `.env.example`；
- 不提交真实密钥、个人数据或收费模型调用结果；
- 测试不得调用收费模型，统一使用 Mock Provider；
- 新增 API 时同步更新 Swagger；
- 不在无关任务中进行大范围重构或目录搬迁；
- 若现有设计有问题，记录影响并做最小范围修复；
- README 和进度表必须反映真实实现，不把 Mock、TODO 或静态页面写成已完成。

## 9. 每阶段完成定义

一项能力只有同时满足以下条件才可标记为“已完成”：

- 前后端真实连通，不依赖仅用于演示的本地 Mock；
- 权限和输入校验在服务端生效；
- 核心成功路径和至少一个失败路径可复现；
- 关键业务逻辑有自动化测试；
- lint、typecheck、相关测试和构建通过；
- 没有把真实密钥写入仓库；
- README 或对应文档包含运行方式；
- 本文件第 6 节的状态和说明已更新。
- 该阶段变更已执行本地 `git add` 和 `git commit`。

若只完成 UI、数据模型、接口外壳或 Mock，只能在说明中记录局部成果，不能把整个阶段标为“已完成”。

## 10. 进度维护规则

每次完成一个阶段性任务后，必须检查并更新第 6 节：

- 只有实际开始编码且形成可验证成果时才设为 `进行中`；
- 只有满足完成定义时才设为 `已完成`；
- 外部依赖或决策导致无法继续时设为 `受阻`，并写明原因；
- 在“当前说明”中记录可运行的最小成果，不记录空泛描述；
- 新需求若偏离第 1、2、7 节，应先说明它与校招作品集目标的关系，再决定是否实施。

## 11. 本地 Git 提交规则

- 根目录使用独立的本地 Git 仓库管理融合目标项目；
- `ai-chat/` 和 `ecommerce-admin/` 必须写入根目录 `.gitignore`，不得被暂存或提交；
- 每完成第 6 节中的一个阶段，自动执行一次本地 `git add` 和 `git commit`，无需再次等待确认；
- 提交前必须先完成该阶段要求的 lint、typecheck、相关测试与构建，并更新第 6 节进度；
- 一个提交只对应一个阶段或该阶段内清晰、可验证的里程碑，禁止把无关改动混入；
- 提交信息使用 Conventional Commits 风格，例如 `feat(api): complete authentication foundation`；
- 若验证未通过，不得为了制造“已完成”记录而提交阶段完成状态；
- 只允许本地提交。未经项目所有者明确要求，不创建远程仓库、不添加 remote、不 push。
